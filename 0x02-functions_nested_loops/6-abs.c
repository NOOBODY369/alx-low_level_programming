#include "main.h"

/**
 * abs - computes the absolute value of an integer.
 *@n: number.
 
 * Return: absolute value of an integer.
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
